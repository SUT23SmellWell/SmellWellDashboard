import { Routes } from '@angular/router';
import { BuilderPage } from './components/builder-page.component'; // Ensure the correct path

export const routes: Routes = [
  { path: 'builder-demo', component: BuilderPage }, // Add the builder-demo route
  // You can add other routes here as needed
];